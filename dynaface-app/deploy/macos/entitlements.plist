<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <!-- Required for binaries built by PyInstaller to run under the hardened
         runtime, which notarization in turn requires.

         The App Store entitlements (app-sandbox, files.user-selected.read-write,
         print, application-identifier, team-identifier) were dropped for Developer
         ID distribution: application-identifier needs a provisioning profile that
         is no longer embedded, and the sandbox only costs file access here. -->
    <key>com.apple.security.cs.allow-jit</key>
    <true/>
    <key>com.apple.security.cs.allow-unsigned-executable-memory</key>
    <true/>
    <key>com.apple.security.cs.disable-library-validation</key>
    <true/>
</dict>
</plist>
